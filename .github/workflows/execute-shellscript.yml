name: Execute Shell Script

# on: push
on:
    workflow_dispatch:

jobs:
    shell-job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Repo
              uses: actions/checkout@v6

            - name: Execute Shell Script
              run: bash ./ascii-script.sh
