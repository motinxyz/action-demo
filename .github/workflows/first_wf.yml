name: First Workflow

on: [push]

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v6
            - name: Welcome Message
              run: |
                  echo "Welcome to GitHub Actions"
                  ls -la
                  cat README.md
