name: First Workflow

on:
    workflow_dispatch:

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v6
            - name: List & Read files
              run: |
                  echo "Welcome to GitHub Actions"
                  ls -la
                  cat README.md
